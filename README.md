Python code that pulls from the RIOT API using their provided API key. 

This code pulls the users name and finds their latest game, displaying important data from the game. 
It requires an up to date API key to run as that is what allows the code to pull data from the API. 
